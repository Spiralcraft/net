show();
originalUrl="/ this is a string, \"decoded\", not 100% valid in a URL /";
encodedUrl=spiralcraft.net.http.URLCodec.encode(originalUrl);
decodedUrl=spiralcraft.net.http.URLCodec.decode(encodedUrl);
originalUrl.equals(decodedUrl);